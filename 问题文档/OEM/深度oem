先查看前端和后端的版本号

​	前端：https://控制器IP/build.txt(curl  https://控制器IP/build.txt)

​	后端：csphere  version

一、更改logo和favicon图标

​		logo.png (注意,要根据实际情况查看图片名称，然后进行替换)
![image]()

​		图片为450x200的svg矢量图,前景色需要浅色,方便在深色背景呈现.

​		favicon.png
![image]()

​		大小为：48x48或64x64

​		1、在Controller控制节点上创建前端目录 `mkdir /etc/csphere/frontend`

​		2、如果需要更改则将设置好的*.png上传到前端目录下

二、深度oem

​		1、将前端压缩包放到/etc/csphere/frontend路径下

​		2、解压前端代码包

​				cd   /etc/csphere/frontend

​				tar   zxf   assets-lite-hidden-menu.tgz

​		3、mv   /etc/csphere/frontend/assets/*     /etc/csphere/frontend/

​		4、在/etc/csphere中创建oem.sh

```
		cd /etc/csphere
```

​					vi   oem.sh

​					



​					chmod   777   oem.sh

​					./oem.sh brand product company

```
		比如：
```

​					./oem.sh 易讯通 ECC 北京易讯通信息技术股份有限公司(要注意product是英文)

